import React from 'react';
import Typography from '@material-ui/core/Typography';

const BlogPost = () => (
  <div style={{ height: '2000px' }}>
    <Typography variant="h1">This is a Blog Post</Typography>
  </div>
);

export default BlogPost;

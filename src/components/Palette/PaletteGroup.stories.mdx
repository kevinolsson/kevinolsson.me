import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import PaletteGroup from './PaletteGroup';
import theme from '../Theme';

<Meta title='Palette' component={PaletteGroup} />

# Palette

<Preview>
  <Story name="Default">
      {Object.keys(theme.palette).map((value, key) => {
        if (typeof theme.palette[value] === 'object') {
          return (
            <PaletteGroup
              key={`row-${key}`}
              value={value}
              palette={theme.palette[value]}
            />
          );
        }
        return null;
      })}
  </Story>
</Preview>
